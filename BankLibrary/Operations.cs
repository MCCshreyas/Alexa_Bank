namespace Alexa_API
{
    public class Operations
    {
        

        public Operations()
        {
        }

        /*
        public int WithdrawMoney(string accoutNumber = "" , string amount = "")
        {
            if (accoutNumber!= "" && amount!= "")
            {
                var money = Convert.ToInt64(amount);

                var balance = Convert.ToInt64(AlexaService.GetCustomer(accoutNumber).Balance);


                if (money > balance)
                {
                    return -1;
                }

                if (money == balance || money < balance)
                {
                    
                }

            }
        }
        */

    }

}