#include<stdio.h>
int main()
{
    a[10],b[10],r,c;
    printf("enter the 4 integer for an array")
    for(r=0,r<3,r++)
    {
        for(c=0,c<3,c++)
        {
            printf("\n Enter the numbers);
            scanf("&a[r][c]);
        }
    

    }

    for(r=0,r<3,r++)
    {
        for(c=0,c<3,c++)
        {
            printf("\n the matrix is","\n",a[r][c]);
        }
    

    }
    return 0;
}